<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CaroNet.Client.WinUI.Views.BestRecordPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. Nút quay lại màn hình chính -->
        <Button x:Name="BackButton" 
                Content="← Quay lại" 
                Grid.Row="0" 
                HorizontalAlignment="Left"
                Click="BackButton_Click"/>

        <!-- 2. Tiêu đề trang -->
        <TextBlock Text="Best Record (Top 10)" 
                   FontSize="32" 
                   FontWeight="Bold" 
                   Grid.Row="1" 
                   Margin="0,16,0,16"/>

        <!-- 3. Danh sách Top 10 người chơi -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ListView x:Name="BestRecordList"
                      ItemsSource="{Binding BestRecords}"
                      SelectionMode="None"
                      MaxWidth="600" 
                      HorizontalAlignment="Center">

                <!-- QUAN TRỌNG: Ép các item con phải kéo giãn hết chiều rộng để đẩy cột 'Wins' về sát bên phải -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,6"
                                Padding="16"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="12"
                                HorizontalAlignment="Stretch">
                            <!-- Đảm bảo border kéo giãn -->

                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="{Binding Rank}" 
                                           FontWeight="Bold" 
                                           FontSize="18"
                                           VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1" 
                                           Text="{Binding PlayerName}" 
                                           FontSize="18"
                                           VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="2" 
                                           HorizontalAlignment="Right" 
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold" 
                                           FontSize="18">
                                    <Run Text="{Binding Wins}"/>
                                    <Run Text=" Wins"/>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>